{% extends 'base.html.twig' %}

{% block title %}Demande de réinitialisation de mot de passe{% endblock %}

{% block body %}
<div class="container mt-5">
    <h2 class="text-center mb-4">🔑 Réinitialisation du mot de passe</h2>

    <p class="text-center">Entrez votre adresse email et nous vous enverrons un lien pour réinitialiser votre mot de passe.</p>

    <div class="card shadow p-4 mx-auto" style="max-width: 400px;">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">{{ message }}</div>
            {% endfor %}
        {% endfor %}

        {{ form_start(requestPassForm) }}
        
        <div class="mb-3">
            {{ form_label(requestPassForm.email, 'Adresse Email', {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(requestPassForm.email, {'attr': {'class': 'form-control', 'placeholder': 'Votre email'}}) }}
            {{ form_errors(requestPassForm.email) }}
        </div>

        <button type="submit" class="btn btn-warning w-100">
            📩 Envoyer le lien de réinitialisation
        </button>

        {{ form_end(requestPassForm) }}
    </div>

    <div class="text-center mt-3">
        <a href="{{ path('app_login') }}">🔙 Retour à la connexion</a>
    </div>
</div>
{% endblock %}